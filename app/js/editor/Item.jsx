import React from 'react'

const Item = (props) => (<div>R:{props.data.radius}, S: {props.data.speed} </div>)
// item.children && item.children.map((item, j) => (
// <div key={j)}>
//     R:{ item.radius }, S: { item.speed } 
// </div>

export default Item